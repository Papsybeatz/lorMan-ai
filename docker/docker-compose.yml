services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: lorman
    ports:
      - "5432:5432"
    volumes:
      - lorman_pgdata:/var/lib/postgresql/data

volumes:
  lorman_pgdata:
