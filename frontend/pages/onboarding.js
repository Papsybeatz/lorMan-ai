import { useState } from 'react';
import Link from 'next/link';
import { jurisdictions, practiceAreas } from '../utils/jurisdiction';

export default function Onboarding() { const [country, setCountry] = useState('Nigeria'); const [areas, setAreas] = useState(['Civil litigation']); return <div className="auth-page"><section className="auth-panel"><div className="brand"><span className="brand-mark">l</span><span>lorMan</span></div><p className="eyebrow">Set up your workspace</p><h1>Make it yours.</h1><p>Your jurisdiction and practice areas shape the prompts used across your workspace.</p><form className="intake-form" onSubmit={(event) => event.preventDefault()}><label>Country<select value={country} onChange={(event) => setCountry(event.target.value)}>{jurisdictions.map((item) => <option key={item}>{item}</option>)}</select></label><label>Practice areas<div className="choice-grid">{practiceAreas.map((item) => <button type="button" className="choice" key={item} onClick={() => setAreas((current) => current.includes(item) ? current.filter((area) => area !== item) : [...current, item])} style={areas.includes(item) ? {borderColor:'var(--teal)', background:'#e9f4dc'} : {}}>{item}</button>)}</div></label><Link href="/dashboard" className="primary-button">Enter workspace <span>-&gt;</span></Link></form></section></div>; }
