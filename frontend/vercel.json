{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "npm run build",
  "installCommand": "npm config delete allow-scripts --location=user && npm config delete allow-scripts --location=global && npm install --ignore-scripts"
}
