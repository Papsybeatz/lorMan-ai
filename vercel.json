{
  "buildCommand": "cd frontend && npm run build",
  "installCommand": "cd frontend && npm config delete allow-scripts --location=user && npm config delete allow-scripts --location=global && npm install --ignore-scripts",
  "outputDirectory": "frontend/.next"
}
